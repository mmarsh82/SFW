<UserControl x:Class="SFW.ShopRoute.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cvtr="clr-namespace:SFW.Converters"
             xmlns:cmd="clr-namespace:SFW.Commands"
             xmlns:main="clr-namespace:SFW"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             x:Name="ShopRouteUserControl">

    <ScrollViewer x:Name="MainScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,0,0,20" VerticalAlignment="Top">
        <Grid AllowDrop="{Binding Path=(main:CurrentUser.CanDeviate)}" Drop="Deviation_Drop">

            <!--#region Resources -->

            <Grid.Resources>
                <cvtr:StringToVisibility x:Key="StringToVisConverter"/>
                <cvtr:StringConcat x:Key="StringCombineConverter"/>
                <cvtr:CountToVisibility x:Key="CountToVisConverter"/>
                <cvtr:SkuMasterPrint x:Key="PartSearchConverter"/>
                <cvtr:BoolToVisibility x:Key="BoolToVisConverter"/>
                <cvtr:DateToVisibility x:Key="DateToVisConverter"/>
                <cvtr:ValueToHeight x:Key="ValueToHeightConverter"/>
                <cvtr:MultiValueParameter x:Key="MultiValConverter"/>
                <cvtr:NegativeIntToBool x:Key="NegIntToBoolConverter"/>
                <cvtr:BoolInverter x:Key="BoolInvertConverter"/>
                <cvtr:WipEnabled x:Key="WipEnabled"/>
                <cmd:ViewLoad x:Key="ViewLoadCommand"/>
                <cmd:M2kLookQuery x:Key="ShowQuery"/>
                <cmd:PartSearch x:Key="PartSearchICommand"/>
                <cmd:SetUpInformation x:Key="SetupCommand"/>
                <cmd:TrimmingInformation x:Key="TrimSetupICommand"/>
                <cmd:M2kWipReceipt x:Key="WipCommand"/>
                <cmd:M2kLabor x:Key="LaborCommand"/>
                <cmd:WorkInstructions x:Key="WorkInstICommand"/>
                <cmd:M2kLookQuery x:Key="LookQueryCommand"/>
                <cmd:LoadNcr x:Key="LoadNcrCommand"/>
                <main:CurrentUser x:Key="User"/>

                <DataGridTextColumn x:Key="dgLotColumn"
                                    Header=" Lot Number  "
                                    Binding="{Binding LotNumber}"
                                    Visibility="{Binding Source={x:Reference MainScroll}, Path=DataContext.ItemIsLotTrace, Converter={StaticResource BoolToVisConverter}}"/>

                <DataGridTextColumn x:Key="dgBomLotColumn"
                                    Header=" Lot Number  "
                                    Binding="{Binding LotNumber}"
                                    Visibility="{Binding Source={x:Reference MainScroll}, Path=DataContext.ItemIsLotTrace, Converter={StaticResource BoolToVisConverter}}"/>

                <DataGridTemplateColumn x:Key="dgDefectColumn"
                                        Header="Defects"
                                        Visibility="{Binding Source={x:Reference MainScroll}, Path=DataContext.ItemIsLotTrace, Converter={StaticResource BoolToVisConverter}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl x:Name="DefectIC"
                                          ItemsSource="{Binding DefectList}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>

                                        <Button Content="{Binding}"
                                                Margin="3,0,3,0"
                                                Width="50"
                                                Command="{StaticResource LoadNcrCommand}"
                                                Style="{DynamicResource DefaultButton}">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource StringCombineConverter}" ConverterParameter="^">
                                                    <Binding />
                                                    <Binding Source="{x:Reference MainScroll}" Path="DataContext.SelectedOrder"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                        </Button>

                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Key="dgBomDefectColumn"
                        Header="Defects"
                        Visibility="{Binding Source={x:Reference MainScroll}, Path=DataContext.ItemIsLotTrace, Converter={StaticResource BoolToVisConverter}}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl x:Name="DefectIC"
                          ItemsSource="{Binding DefectList}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel IsItemsHost="True" Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>

                                        <Button Content="{Binding}"
                                                Margin="3,0,3,0"
                                                Width="50"
                                                Command="{StaticResource LoadNcrCommand}"
                                                Style="{DynamicResource DefaultButton}">
                                            <Button.CommandParameter>
                                                <MultiBinding Converter="{StaticResource StringCombineConverter}" ConverterParameter="^">
                                                    <Binding />
                                                    <Binding Source="{x:Reference MainScroll}" Path="DataContext.SelectedOrder"/>
                                                </MultiBinding>
                                            </Button.CommandParameter>
                                        </Button>
                                        
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>

                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </Grid.Resources>

            <!--#endregion-->

            <TextBlock Text="           Nothing selected!&#x0a;Select a work order to review."
                       FontSize="32"
                       Margin="0,100,0,0"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="{Binding ShopOrder.OrderNumber, Converter={StaticResource StringToVisConverter}, ConverterParameter=i}"/>

            <Button Content="Start Setup"
                    FontSize="24"
                    Width="200"
                    Height="40"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="Hidden"
                    Style="{DynamicResource DefaultButton}"/>

            <Button Content="Skip Setup"
                    FontSize="24"
                    Width="200"
                    Height="40"
                    Margin="0,100,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="Hidden"
                    Style="{DynamicResource DefaultButton}"/>
            
            <!--#region Work Order Comments -->

            <Expander ExpandDirection="Left"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Stretch"
                      Margin="0,5,0,5"
                      IsExpanded="False"
                      Panel.ZIndex="1">

                <Expander.Background>
                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                        <GradientStop Color="#FF6B8E95" Offset="0"/>
                        <GradientStop Color="#FF14A7C1" Offset="1"/>
                        <GradientStop Color="#FF1E424E" Offset="0.509"/>
                        <GradientStop Color="#FF1D4855" Offset="0.542"/>
                        <GradientStop Color="#FF193A44" Offset="0.526"/>
                    </LinearGradientBrush>
                </Expander.Background>

                <Expander.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Operator Comments"
                                   FontWeight="Bold"
                                   FontSize="18"
                                   Foreground="WhiteSmoke"
                                   Tag="{x:Static RotateTransform.AngleProperty}"
                                   RenderTransformOrigin="0.5,0.5">
                            <TextBlock.LayoutTransform>
                                <RotateTransform Angle="-90"/>
                            </TextBlock.LayoutTransform>
                        </TextBlock>
                    </StackPanel>
                </Expander.Header>

                <!-- Comment content -->
                <ScrollViewer Background="LightGray"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                    <Grid Width="500">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="75"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBox Grid.Row="0"
                                 BorderBrush="Black"
                                 Text="{Binding CommentInput, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                 BorderThickness="1"
                                 FontSize="14"
                                 AcceptsReturn="True"
                                 AcceptsTab="True"
                                 SpellCheck.IsEnabled="True"
                                 Visibility="{Binding Path=(main:CurrentUser.IsLoggedIn), Converter={StaticResource BoolToVisConverter}}"
                                 MaxLength="255"/>

                        <Button Grid.Row="1"
                                HorizontalAlignment="Right"
                                Content="Add Note"
                                Command="{Binding ModifyCommentICommand}"
                                CommandParameter="A"
                                Margin="0,0,3,0"
                                Visibility="{Binding Path=(main:CurrentUser.IsLoggedIn), Converter={StaticResource BoolToVisConverter}}"
                                Style="{DynamicResource MenuButtonStyle}"/>

                        <ItemsControl Name="CommentsIC"
                                      Grid.Row="2"
                                      ItemsSource="{Binding CommentCollection, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                      Margin="10,0,10,0"
                                      FontSize="18"
                                      Cursor="Hand"
                                      Background="Transparent"
                                      Foreground="Black"
                                      Focusable="False">

                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel IsItemsHost="True"
                                                            Orientation="Vertical" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>

                                    <Grid>

                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="20"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0"
                                                    Orientation="Horizontal">
                                            <StackPanel.Visibility>
                                                <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="1|0">
                                                    <Binding Path="(main:CurrentUser.IsSupervisor)"/>
                                                    <Binding Path="IsHidden"/>
                                                </MultiBinding>
                                            </StackPanel.Visibility>

                                            <Button Command="{Binding DataContext.ModifyCommentICommand, ElementName=ShopRouteUserControl}"
                                                    CommandParameter="{Binding}"
                                                    Margin="1"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Style="{DynamicResource DefaultButton}">
                                                <Button.Content>
                                                    <Image Source="/Icons/Show.ico" />
                                                </Button.Content>
                                                <Button.Visibility>
                                                    <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="1*0">
                                                        <Binding Path="(main:CurrentUser.IsSupervisor)"/>
                                                        <Binding Path="IsHidden"/>
                                                    </MultiBinding>
                                                </Button.Visibility>
                                            </Button>

                                            <Button Command="{Binding DataContext.ModifyCommentICommand, ElementName=ShopRouteUserControl}"
                                                    CommandParameter="{Binding}"
                                                    Margin="1"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Style="{DynamicResource DefaultButton}">
                                                <Button.Content>
                                                    <Image Source="/Icons/Hide.ico" />
                                                </Button.Content>
                                                <Button.Visibility>
                                                    <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="1*1">
                                                        <Binding Path="(main:CurrentUser.IsSupervisor)"/>
                                                        <Binding Path="IsHidden"/>
                                                    </MultiBinding>
                                                </Button.Visibility>
                                            </Button>

                                            <TextBlock Text="{Binding SubmitterId}"
                                                       Margin="10,0,0,0"
                                                       FontStyle="Italic"
                                                       FontWeight="Bold"/>

                                            <TextBlock Text="{Binding SubmitterName}"
                                                       Margin="5,0,0,0"
                                                       FontStyle="Italic"
                                                       FontWeight="Bold"/>

                                            <TextBlock Text="{Binding SubmitDate, StringFormat='{}{0:ddMMMyyyy HH:mm}'}"
                                                       Margin="20,0,0,0"
                                                       FontStyle="Italic"
                                                       FontWeight="Bold"/>

                                        </StackPanel>

                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding CommentText}">
                                            <TextBlock.Visibility>
                                                <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="1|0">
                                                    <Binding Path="(main:CurrentUser.IsSupervisor)"/>
                                                    <Binding Path="IsHidden"/>
                                                </MultiBinding>
                                            </TextBlock.Visibility>
                                        </TextBlock>

                                    </Grid>

                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                        </ItemsControl>

                    </Grid>
                </ScrollViewer>
            </Expander>
            
            <!--#endregion-->

            <!-- <Grid Margin="10,0,10,0" Visibility="Hidden"> -->
            <Grid Panel.ZIndex="0" Margin="10,0,10,0" Visibility="{Binding ShopOrder.OrderNumber, Converter={StaticResource StringToVisConverter}, ConverterParameter=Hide}">

                <Grid.RowDefinitions>
                    <RowDefinition x:Name="Row0" Height="40"/>
                    <RowDefinition x:Name="Row1" Height="50"/>
                    <RowDefinition x:Name="Row2" Height="30"/>
                    <RowDefinition x:Name="Row3" Height="30"/>
                    <RowDefinition x:Name="Row4" Height="10"/>
                    <RowDefinition x:Name="Row5" Height="90"/>
                    <RowDefinition x:Name="Row6" Height="40"/>
                    <RowDefinition x:Name="Row7" Height="200"/>
                    <RowDefinition x:Name="Row8" Height="{Binding ShopOrder.SalesOrder.SalesNumber, Converter={StaticResource ValueToHeightConverter}, ConverterParameter=40}"/>
                    <RowDefinition x:Name="Row9" Height="{Binding ShopOrder.SalesOrder.SalesNumber, Converter={StaticResource ValueToHeightConverter}, ConverterParameter=30}"/>
                    <RowDefinition x:Name="Row10" Height="{Binding ShopOrder.SalesOrder.InternalComments, Converter={StaticResource ValueToHeightConverter}, ConverterParameter=L30}"/>
                    <RowDefinition x:Name="Row11" Height="Auto" />
                </Grid.RowDefinitions>

                <!--#region Grid.Row 0  Command Array-->

                <StackPanel Name="CommandArrayStackPanel" Grid.Row="0"
                            Orientation="Horizontal">
                    <Button Name="IthButton"
                            Content="ITH"
                            FontSize="16"
                            Visibility="{Binding CanCheckHistory, Converter={StaticResource BoolToVisConverter}}"
                            Command="{StaticResource ViewLoadCommand}"
                            CommandParameter="{Binding ShopOrder}"
                            Style="{DynamicResource MenuButtonStyle}" />
                    <Button Name="WipButton"
                            Content="Wip Receipt"
                            FontSize="16"
                            Width="110"
                            Command="{StaticResource WipCommand}"
                            CommandParameter="{Binding ShopOrder}"
                            Visibility="{Binding Path=(main:CurrentUser.CanWip), Converter={StaticResource BoolToVisConverter}}"
                            Style="{DynamicResource MenuButtonStyle}">
                        <Button.IsEnabled>
                            <MultiBinding Converter="{StaticResource WipEnabled}">
                                <Binding Path="WipActive"/>
                                <Binding Path="(main:CurrentUser.CanWip)"/>
                                <Binding Path="(main:ApplicationTimer.Status)"/>
                            </MultiBinding>
                        </Button.IsEnabled>
                    </Button>
                    <Button Name="LaborButton"
                            Content="Enter Labor"
                            FontSize="16"
                            Width="110"
                            IsEnabled="{Binding Path=(main:CurrentUser.CanLabor)}"
                            Command="{StaticResource LaborCommand}"
                            CommandParameter="{Binding ShopOrder}"
                            Visibility="Collapsed"
                            Style="{DynamicResource MenuButtonStyle}"/>
                    <Button Name="SetUpButton"
                            Content="Set-up Info"
                            FontSize="16"
                            Command="{StaticResource SetupCommand}"
                            CommandParameter="{Binding SetupFile}"
                            Visibility="{Binding HasSetupFile, Converter={StaticResource BoolToVisConverter}}"
                            Style="{DynamicResource MenuButtonStyle}" />
                    <Button Name="TrimButton"
                            Content="Trim Info"
                            FontSize="16"
                            Command="{StaticResource TrimSetupICommand}"
                            CommandParameter="{Binding ShopOrder.Product.SkuNumber}"
                            Visibility="{Binding CanSeeTrim, Converter={StaticResource BoolToVisConverter}}"
                            Style="{DynamicResource MenuButtonStyle}" />
                    <Menu Style="{DynamicResource ModernMenu}"
                          Visibility="{Binding ShopOrder.Product.InstructionList.Count, Converter={StaticResource CountToVisConverter}}">
                        <MenuItem Header="Work&#10;Instructions"
                                  ItemsSource="{Binding ShopOrder.Product.InstructionList}"
                                  FontSize="14"
                                  Width="110"
                                  Height="39"
                                  Style="{DynamicResource ModernMenuItem}">
                            <MenuItem.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Command"
                                            Value="{StaticResource WorkInstICommand}" />
                                    <Setter Property="CommandParameter"
                                            Value="{Binding}" />
                                    <Setter Property="Foreground"
                                            Value="Black"/>
                                </Style>
                            </MenuItem.ItemContainerStyle>
                        </MenuItem>
                    </Menu>
                    <Menu Style="{DynamicResource ModernMenu}"
                          Visibility="{Binding ShopOrder.ToolList.Count, Converter={StaticResource CountToVisConverter}}">
                        <MenuItem Header="Tools and Dies"
                                  ItemsSource="{Binding ShopOrder.ToolList}"
                                  DisplayMemberPath="ToolID"
                                  FontSize="14"
                                  Width="110"
                                  Height="39"
                                  Style="{DynamicResource ModernMenuItem}">
                            <MenuItem.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Command"
                                            Value="{StaticResource PartSearchICommand}" />
                                    <Setter Property="CommandParameter"
                                            Value="{Binding ToolID}" />
                                    <Setter Property="Foreground"
                                            Value="Black"/>
                                </Style>
                            </MenuItem.ItemContainerStyle>
                        </MenuItem>
                    </Menu>
                    <Button Content="Record Defect"
                            FontSize="16"
                            Width="120"
                            Height="39"
                            Visibility="{Binding Path=(main:CurrentUser.IsQuality), Converter={StaticResource BoolToVisConverter}}"
                            Command="{StaticResource ViewLoadCommand}"
                            Style="{DynamicResource MenuButtonStyle}">
                        <Button.IsEnabled>
                            <MultiBinding Converter="{StaticResource WipEnabled}">
                                <Binding Path="(main:CurrentUser.IsQuality)"/>
                                <Binding Path="(main:ApplicationTimer.Status)"/>
                            </MultiBinding>
                        </Button.IsEnabled>
                        <Button.CommandParameter>
                            <MultiBinding Converter="{StaticResource StringCombineConverter}" ConverterParameter="NCR">
                                <Binding Path="ShopOrder.OrderNumber"/>
                            </MultiBinding>
                        </Button.CommandParameter>
                    </Button>
                    <Menu Style="{DynamicResource ModernMenu}"
                          Visibility="{Binding NcrList.Count, Converter={StaticResource CountToVisConverter}}">
                        <MenuItem Header="View Defects"
                                  ItemsSource="{Binding NcrList}"
                                  FontSize="16"
                                  Width="110"
                                  Height="39"
                                  Style="{DynamicResource ModernMenuItem}">
                            <MenuItem.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Command"
                                            Value="{StaticResource LoadNcrCommand}" />
                                    <Setter Property="CommandParameter"
                                            Value="{Binding}" />
                                    <Setter Property="Foreground"
                                            Value="Black"/>
                                </Style>
                            </MenuItem.ItemContainerStyle>
                        </MenuItem>
                    </Menu>

                </StackPanel>

                <!--#endregion-->

                <!--#region Grid.Row 1  Work Order Header-->

                <StackPanel Grid.Row="1" Orientation="Horizontal"
                            HorizontalAlignment="Left" VerticalAlignment="Center">

                    <TextBlock Text="Shop Routing For Work Order:"
                               FontSize="32"
                               FontWeight="Bold"
                               Foreground="#FF135185"
                               TextDecorations="Underline"/>

                    <TextBlock x:Name="tbOrderNumber"
                               Text="{Binding ShopOrder.OrderNumber}"
                               FontSize="32"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                    <TextBlock Text="{Binding ShopOrder.SalesOrder.LoadPattern}"
                               Foreground="Crimson"
                               FontSize="32"
                               FontWeight="Bold"
                               TextDecorations="Underline"
                               Margin="30,0,0,0" />

                </StackPanel>

                <!--#endregion-->

                <!--#region Grid.Row 2 Operation Description-->

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal">

                    <TextBlock Text="Routing:"
                               FontSize="20"
                               FontWeight="Bold"
                               TextDecorations="UnderLine"
                               Foreground="#FF135185" />

                    <TextBlock Text="{Binding ShopOrder.Routing}"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="10,0,30,0" />
                    
                    <TextBlock Text="Operation:"
                               FontSize="20"
                               FontWeight="Bold"
                               TextDecorations="UnderLine"
                               Foreground="#FF135185" />

                    <TextBlock Text="{Binding ShopOrder.Product.Operation}"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                    <TextBlock Text="{Binding ShopOrder.OpDesc}"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                </StackPanel>

                <!--#endregion-->

                <!--#region Grid.Row 3  Part Information-->

                <StackPanel Grid.Row="3" Orientation="Horizontal">

                    <Button Margin="0,0,5,0"
                            Width="30"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{StaticResource ViewLoadCommand}"
                            CommandParameter="{Binding ShopOrder.Product.SkuNumber}"
                            Style="{DynamicResource DefaultButton}">
                        <Image Source="/SFW;component/Icons/Search.ico"
                               Margin="1"/>
                    </Button>

                    <TextBlock Text="Part Number:"
                           FontSize="24"
                           FontWeight="Bold"
                           TextDecorations="UnderLine"
                           Foreground="#FF135185"/>

                    <TextBlock Text="{Binding ShopOrder.Product.SkuNumber}"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="10,0,0,0"/>

                    <TextBlock Text="{Binding ShopOrder.Product.SkuDescription}"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="10,0,0,0"/>

                    <TextBlock Text="Unit of Measure:"
                           FontSize="24"
                           FontWeight="Bold"
                           Margin="50,0,0,0"
                           TextDecorations="Underline"
                           Foreground="#FF135185"/>

                    <TextBlock Text="{Binding ShopOrder.Product.Uom}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                </StackPanel>

                <!--#endregion-->

                <!--#region Grid.Row 4 (Used as a spacer)-->
                <!--#endregion-->

                <!--#region Grid.Row 5  Date and Quantity-->

                <Grid Grid.Row="5">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="225"/>
                        <ColumnDefinition Width="225"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0"
                          Visibility="{Binding IsPlan, Converter={StaticResource BoolToVisConverter}, ConverterParameter=i}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <Rectangle Grid.Row="1" Grid.RowSpan="2"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Stroke="Black"
                                   StrokeThickness="1"
                                   Margin="1,0,5,2"
                                   Panel.ZIndex="0"/>

                        <Rectangle Grid.Row="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Stroke="Black"
                                   StrokeThickness="1"
                                   Margin="1,0,5,0"
                                   Panel.ZIndex="0">
                            <Rectangle.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="#FF6B8E95" Offset="0"/>
                                    <GradientStop Color="#FF14A7C1" Offset="1"/>
                                    <GradientStop Color="#FF1E424E" Offset="0.509"/>
                                    <GradientStop Color="#FF1D4855" Offset="0.542"/>
                                    <GradientStop Color="#FF193A44" Offset="0.526"/>
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>

                        <TextBlock Grid.Row="0"
                                   Text="Scheduled"
                                   FontSize="20"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   FontWeight="Bold"
                                   Foreground="White"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal">

                            <TextBlock Text="Start:"
                                       FontSize="18"
                                       Margin="5,0,0,0"
                                       TextDecorations="Underline"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.SchedStartDate, StringFormat=d}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal">

                            <TextBlock Text="Due:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="5,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.DueDate, StringFormat=d}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <Rectangle Grid.Row="1" Grid.RowSpan="2"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Stroke="Black"
                                   StrokeThickness="1"
                                   Margin="1,0,5,2"
                                   Panel.ZIndex="0"/>

                        <Rectangle Grid.Row="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Stroke="Black"
                                   StrokeThickness="1"
                                   Margin="1,0,5,0"
                                   Panel.ZIndex="0">
                            <Rectangle.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="#FF6B8E95" Offset="0"/>
                                    <GradientStop Color="#FF14A7C1" Offset="1"/>
                                    <GradientStop Color="#FF1E424E" Offset="0.509"/>
                                    <GradientStop Color="#FF1D4855" Offset="0.542"/>
                                    <GradientStop Color="#FF193A44" Offset="0.526"/>
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>

                        <TextBlock Grid.Row="0"
                                   Text="Original"
                                   FontSize="20"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"
                                   FontWeight="Bold"
                                   Foreground="White"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal">

                            <TextBlock Text="Start:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="5,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.OriginStartDate, StringFormat=d}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal">

                            <TextBlock Text="Due:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="5,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.OriginDueDate, StringFormat=d}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <Rectangle Grid.Row="1" Grid.RowSpan="2"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Stroke="Black"
                                  StrokeThickness="1"
                                  Margin="1,0,5,2"
                                  Panel.ZIndex="0"/>

                        <Rectangle Grid.Row="0"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Stroke="Black"
                                  StrokeThickness="1"
                                  Margin="1,0,5,0"
                                  Panel.ZIndex="0">
                            <Rectangle.Fill>
                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                    <GradientStop Color="#FF6B8E95" Offset="0"/>
                                    <GradientStop Color="#FF14A7C1" Offset="1"/>
                                    <GradientStop Color="#FF1E424E" Offset="0.509"/>
                                    <GradientStop Color="#FF1D4855" Offset="0.542"/>
                                    <GradientStop Color="#FF193A44" Offset="0.526"/>
                                </LinearGradientBrush>
                            </Rectangle.Fill>
                        </Rectangle>

                        <TextBlock Grid.Row="0"
                                  Text="Quantity"
                                  FontSize="20"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  FontWeight="Bold"
                                  Foreground="White"/>

                        <StackPanel Grid.Row="1" Orientation="Horizontal">

                            <TextBlock Text="Required:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="10,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.StartQty}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal">

                            <TextBlock Text="Balance:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="10,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.CurrentQty}"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="10,0,0,0"/>

                        </StackPanel>
                    </Grid>

                    <StackPanel Grid.Column="3" Orientation="Vertical" VerticalAlignment="Center">

                        <StackPanel Orientation="Horizontal" Visibility="{Binding ShopOrder.Product.CustomerRev, Converter={StaticResource StringToVisConverter}}">

                            <TextBlock Text="Customer Revision:"
                                   FontSize="18"
                                       Margin="10,0,0,0"
                                   TextDecorations="Underline"
                                   Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.Product.CustomerRev}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="10,0,0,0"/>

                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Visibility="{Binding ShopOrder.Product.InternalRev, Converter={StaticResource StringToVisConverter}}">

                            <TextBlock Text="Internal Revision:"
                                       FontSize="18"
                                       TextDecorations="Underline"
                                       Margin="10,0,0,0"
                                       Foreground="#FF135185"/>

                            <TextBlock Text="{Binding ShopOrder.Product.InternalRev}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="10,0,0,0"/>

                        </StackPanel>
                    </StackPanel>

                </Grid>

                <!--#endregion-->

                <!--#region Grid.Row 6  Inspection Warning-->

                <TextBlock Grid.Row="6"
                           Text="* 1ST PIECE SAMPLE MUST BE TURNED IN *"
                           FontSize="28"
                           FontWeight="Bold"
                           FontStyle="Italic"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="Crimson"
                           Visibility="{Binding HasFirstPiece, Converter={StaticResource BoolToVisConverter}}"
                           Panel.ZIndex="3"/>

                <Rectangle Grid.Row="6"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Stroke="Crimson"
                           StrokeThickness="2"
                           StrokeDashArray="4,4"
                           Visibility="{Binding HasFirstPiece, Converter={StaticResource BoolToVisConverter}}"
                           Panel.ZIndex="2"/>

                <TextBlock Grid.Row="6"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Visibility="{Binding HasFirstPiece, Converter={StaticResource BoolToVisConverter}}"
                           Panel.ZIndex="1">
                    <TextBlock.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Color="White" Offset="0.25"/>
                            <GradientStop Color="Bisque" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Background>
                </TextBlock>

                <!--#endregion-->

                <!--#region Grid.Row 7  Notes-->

                <Grid Grid.Row="7">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="500"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="500"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="140"/>
                        <RowDefinition Height="25"/>
                    </Grid.RowDefinitions>

                    <Rectangle Grid.Column="0" Grid.Row="0"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                               Stroke="Black"
                               StrokeThickness="1"
                               Panel.ZIndex="0">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Color="#FF6B8E95" Offset="0"/>
                                <GradientStop Color="#FF14A7C1" Offset="1"/>
                                <GradientStop Color="#FF1E424E" Offset="0.509"/>
                                <GradientStop Color="#FF1D4855" Offset="0.542"/>
                                <GradientStop Color="#FF193A44" Offset="0.526"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>

                    <TextBlock Grid.Column="0" Grid.Row="0"
                               Text="Work Order Notes"
                               FontSize="18"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="WhiteSmoke"/>

                    <Button Name="SaveButton"
                            Grid.Column="0" Grid.Row="0"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding WONoteChgICommand}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsFocused, Converter={StaticResource BoolToVisConverter}, ElementName=WoNotesTextBox}"
                            Style="{DynamicResource DefaultButton}">
                        <Image Source="/SFW;component/Icons/Accept.ico" Margin="2,2,5,2"/>
                    </Button>

                    <TextBox Name="WoNotesTextBox"
                             Grid.Column="0" Grid.Row="1"
                             Text="{Binding ShopOrderNotes, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, FallbackValue={x:Null}}"
                             FontSize="18"
                             BorderBrush="Black"
                             TextWrapping="WrapWithOverflow"
                             VerticalScrollBarVisibility="Auto"
                             Focusable="{Binding Path=(main:CurrentUser.CanSchedule)}"
                             AcceptsReturn="True"/>

                    <Rectangle Grid.Column="2" Grid.Row="0"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                               Stroke="Black"
                               StrokeThickness="1"
                               Panel.ZIndex="0">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                <GradientStop Color="#FF6B8E95" Offset="0"/>
                                <GradientStop Color="#FF14A7C1" Offset="1"/>
                                <GradientStop Color="#FF1E424E" Offset="0.509"/>
                                <GradientStop Color="#FF1D4855" Offset="0.542"/>
                                <GradientStop Color="#FF193A44" Offset="0.526"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>

                    <TextBlock Grid.Column="2" Grid.Row="0"
                               Text="Shop Floor Notes"
                               FontSize="18"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="WhiteSmoke"/>

                    <TextBox Name="ShopNotesTextBox"
                             Grid.Column="2" Grid.Row="1"
                             Text="{Binding ShopOrder.ShopNotes}"
                             FontSize="18"
                             BorderBrush="Black"
                             TextWrapping="WrapWithOverflow"
                             Focusable="False"/>

                </Grid>

                <!--#endregion-->

                <!--#region Grid.Row 8  Sales Order Header-->

                <StackPanel Grid.Row="8" Orientation="Horizontal"
                        HorizontalAlignment="Left" VerticalAlignment="Center">

                    <Button Name="SO"
                            FontSize="18"
                            Margin="0,0,5,0"
                            Width="30"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{StaticResource ShowQuery}"
                            Style="{DynamicResource DefaultButton}">
                        <Image Source="/SFW;component/Icons/Search.ico"
                               Margin="1"/>
                        <Button.CommandParameter>
                            <MultiBinding Converter="{StaticResource MultiValConverter}">
                                <Binding Path="Name" RelativeSource="{RelativeSource Self}"/>
                                <Binding Path="FqSalesOrder"/>
                            </MultiBinding>
                        </Button.CommandParameter>
                    </Button>

                    <TextBlock Text="Sales Order:"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#FF135185"
                               TextDecorations="Underline"/>

                    <TextBlock Text="{Binding ShopOrder.SalesOrder.SalesNumber}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                    <TextBlock Text="Customer:"
                               FontSize="24"
                               FontWeight="Bold"
                               TextDecorations="Underline"
                               Foreground="#FF135185" 
                               Margin="20,0,0,0"/>

                    <TextBlock Text="{Binding ShopOrder.SalesOrder.FullCustomerName}"
                               FontSize="24"
                               FontWeight="Bold"
                               Margin="10,0,0,0" />

                </StackPanel>

                <!--#endregion-->

                <!--#region Grid.Row 9  Sales Order Details-->

                <Grid Grid.Row="9">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="170"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">

                        <TextBlock Text="Line Item:"
                               FontSize="18"
                               TextDecorations="Underline"
                               Foreground="#FF135185"/>

                        <TextBlock Text="{Binding ShopOrder.SalesOrder.LineNumber}"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="10,0,0,0"/>

                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal">

                        <TextBlock Text="Quantity:"
                                   FontSize="18"
                                   TextDecorations="Underline"
                                   Foreground="#FF135185" />

                        <TextBlock Text="{Binding ShopOrder.SalesOrder.LineBalQuantity}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="10,0,0,0" />

                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Visibility="{Binding ShopOrder.SalesOrder.CustomerPart, Converter={StaticResource StringToVisConverter}}">

                        <TextBlock Text="Customer Part Number:"
                               FontSize="18"
                               TextDecorations="Underline"
                               Foreground="#FF135185"/>

                        <TextBlock Text="{Binding ShopOrder.SalesOrder.CustomerPart}"
                               FontSize="18"
                               FontWeight="Bold"
                               Margin="10,0,0,0"/>

                    </StackPanel>

                </Grid>

                <!--#endregion-->

                <!--#region Grid.Row 10  Sales Order Comments-->

                <Grid Grid.Row="10">

                    <StackPanel Orientation="Horizontal">

                        <TextBlock Text="Internal Comments:"
                                   FontSize="18"
                                   TextDecorations="Underline"
                                   Foreground="#FF135185"/>

                        <TextBlock Text="{Binding ShopOrder.SalesOrder.InternalComments}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="Crimson"
                                   Margin="10,0,0,0"/>

                    </StackPanel>

                </Grid>

                <!--#endregion-->

                <!--#region Grid.Row 11  Tab Control for Work order PickList and BOM-->

                <Grid Grid.Row="11" MaxHeight="360">

                    <Control Background="Transparent"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Margin="0,50,0,0"
                             Visibility="{Binding IsMultiLoading, Converter={StaticResource BoolToVisConverter}}"
                             Style="{DynamicResource BusyAnimationStyle}" />

                    <TabControl Background="Transparent"
                                BorderThickness="0"
                                Visibility="{Binding IsMultiLoading, Converter={StaticResource BoolToVisConverter}, ConverterParameter=i}"
                                Style="{DynamicResource ModernTabControl}">

                        <!--#region Picklist Tab-->

                        <TabItem Header="Picklist Parts"
                                 FontSize="18"
                                 Visibility="{Binding ShopOrder.PickList.Count, Converter={StaticResource CountToVisConverter}}"
                                 IsSelected="{Binding BomOnly, Converter={StaticResource BoolInvertConverter}}"
                                 Style="{DynamicResource ModernTabItem}">

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="500"/>
                                    <ColumnDefinition Width="350"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="20"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!--#region PickList DataGrid-->

                                <DataGrid Grid.ColumnSpan="3" Grid.Row="0"
                                          x:Name="dgPick"
                                          ItemsSource="{Binding ShopOrder.PickList}"
                                          SelectedItem="{Binding SelectedILotItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsReadOnly="True"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          AlternatingRowBackground="LightGray"
                                          SelectionMode="Single"
                                          RowDetailsVisibilityMode="Visible"
                                          AutoGenerateColumns="False"
                                          RowHeaderWidth="0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Hidden"
                                          ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">

                                    <DataGrid.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                                         Color="CadetBlue"/>
                                    </DataGrid.Resources>

                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="            Part Number ">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Command="{StaticResource PartSearchICommand}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Style="{DynamicResource ResourceKey=DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/ProductSearch.ico" />
                                                            <Button.CommandParameter>
                                                                <MultiBinding Converter="{StaticResource PartSearchConverter}">
                                                                    <Binding Path="ProductNumber" />
                                                                    <Binding Path="ProductMasterPrint" />
                                                                </MultiBinding>
                                                            </Button.CommandParameter>
                                                        </Button>
                                                        <Button Command="{StaticResource ViewLoadCommand}"
                                                                CommandParameter="{Binding ProductNumber}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Style="{DynamicResource DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/Search.ico" />
                                                        </Button>
                                                        <Button Name="ACT"
                                                                Command="{StaticResource LookQueryCommand}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Visibility="Collapsed"
                                                                Style="{DynamicResource DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/Help.ico" />
                                                            <Button.CommandParameter>
                                                                <MultiBinding Converter="{StaticResource MultiValConverter}">
                                                                    <Binding Path="Name" RelativeSource="{RelativeSource Self}"/>
                                                                    <Binding Path="ProductNumber"/>
                                                                </MultiBinding>
                                                            </Button.CommandParameter>
                                                        </Button>
                                                        <TextBlock Text="{Binding ProductNumber}"
                                                                   Margin="20,0,5,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header=" Description"
                                                            Binding="{Binding ProductDescription}" />
                                        <DataGridTextColumn Header=" Assembly ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="AssemblyQuantity" />
                                                    <Binding Path="ProductUom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header=" On Hand ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="Stock" />
                                                    <Binding Path="ProductUom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header=" Required ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="RequiredQuantity" />
                                                    <Binding Path="ProductUom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header=" Scrap Factor "
                                                            Binding="{Binding ScrapFactor, StringFormat={}{0:P}}" />
                                    </DataGrid.Columns>

                                </DataGrid>

                                <!--#endregion-->
                                
                                <!--#region PickList Lot Datagrid -->

                                <Grid Grid.ColumnSpan="2" Grid.Row="2">

                                    <Control Background="Transparent"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Center"
                                             Margin="0,50,0,0"
                                             Visibility="{Binding LotListLoading, Converter={StaticResource BoolToVisConverter}}"
                                             Style="{DynamicResource BusyAnimationStyle}" />

                                    <TextBlock Text="{Binding LotListText}"
                                               FontSize="24"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding LotList.Count, Converter={StaticResource CountToVisConverter}}"/>

                                    <DataGrid x:Name="dgLotResult"
                                              ItemsSource="{Binding ILotResultsList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Top"
                                              MaxWidth="500"
                                              FontSize="18"
                                              FontWeight="Bold"
                                              RowHeaderWidth="0"
                                              AlternatingRowBackground="LightGray"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">
                                        <DataGrid.Visibility>
                                            <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="0*0">
                                                <Binding Path="NoLotResults"/>
                                                <Binding Path="LotListLoading"/>
                                            </MultiBinding>
                                        </DataGrid.Visibility>
                                        <DataGrid.Columns>
                                            <StaticResource ResourceKey="dgLotColumn"/>
                                            <DataGridTextColumn Header=" On Hand "
                                                                Binding="{Binding Onhand}">
                                                <DataGridTextColumn.CellStyle>
                                                    <Style TargetType="{x:Type DataGridCell}">
                                                        <Setter Property="Foreground"
                                                                Value="Black" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Onhand, Converter={StaticResource NegIntToBoolConverter}}"
                                                                         Value="true">
                                                                <Setter Property="Foreground"
                                                                        Value="Crimson" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </DataGridTextColumn.CellStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header=" Location "
                                                                Binding="{Binding Location}" />
                                            <StaticResource ResourceKey="dgDefectColumn"/>

                                        </DataGrid.Columns>

                                    </DataGrid>

                                </Grid>
                                
                                <!--#endregion-->

                                <!--#region PickList Dedicate Datagrid-->

                                <DataGrid Grid.Column="1" Grid.Row="2"
                                            ItemsSource="{Binding IDedicateLotResultsList}"
                                            Visibility="{Binding NoDedicateResults, ConverterParameter=i, Converter={StaticResource BoolToVisConverter}}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Top"
                                            FontSize="18"
                                          RowHeaderWidth="0"
                                            FontWeight="Bold"
                                            AlternatingRowBackground="LightGray"
                                            AutoGenerateColumns="False"
                                            IsReadOnly="True"
                                            ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header=" Lot Number "
                                                                Binding="{Binding LotNumber}" />
                                        <DataGridTextColumn Header=" Dedicated ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="Onhand" />
                                                    <Binding Path="Uom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                            <DataGridTextColumn.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Setter Property="Foreground"
                                                                Value="Black" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Onhand, Converter={StaticResource NegIntToBoolConverter}}"
                                                                         Value="true">
                                                            <Setter Property="Foreground"
                                                                        Value="Crimson" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGridTextColumn.CellStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header=" Location "
                                            Binding="{Binding Location}" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!--#endregion-->

                            </Grid>

                        </TabItem>

                        <!--#endregion-->

                        <!--#region Bill of Material Tab-->

                        <TabItem Header="Bill of Material (BOM)"
                                 FontSize="18"
                                 IsSelected="{Binding BomOnly}"
                                 Style="{DynamicResource ModernTabItem}">

                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!--#region Bill of Material Grid-->

                                <DataGrid x:Name="dgBom"
                                          ItemsSource="{Binding ShopOrder.BillList}"
                                          SelectedItem="{Binding SelectedILotItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          IsReadOnly="True"
                                          Focusable="False"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          AlternatingRowBackground="LightGray"
                                          SelectionMode="Single"
                                          RowDetailsVisibilityMode="Visible"
                                          AutoGenerateColumns="False"
                                          RowHeaderWidth="0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Hidden"
                                          ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">

                                    <DataGrid.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                                         Color="CadetBlue"/>
                                    </DataGrid.Resources>

                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Header="            Part Number ">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal">
                                                        <Button Command="{StaticResource PartSearchICommand}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Style="{DynamicResource ResourceKey=DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/ProductSearch.ico" />
                                                            <Button.CommandParameter>
                                                                <MultiBinding Converter="{StaticResource PartSearchConverter}">
                                                                    <Binding Path="ProductNumber" />
                                                                    <Binding Path="ProductMasterPrint" />
                                                                </MultiBinding>
                                                            </Button.CommandParameter>
                                                        </Button>
                                                        <Button Command="{StaticResource ViewLoadCommand}"
                                                                CommandParameter="{Binding ProductNumber}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Style="{DynamicResource DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/Search.ico" />
                                                        </Button>
                                                        <Button Name="ACT"
                                                                Command="{StaticResource LookQueryCommand}"
                                                                Height="20"
                                                                Width="40"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Visibility="Collapsed"
                                                                Style="{DynamicResource DefaultButton}">
                                                            <Image Source="/SFW;component/Icons/Help.ico" />
                                                            <Button.CommandParameter>
                                                                <MultiBinding Converter="{StaticResource MultiValConverter}">
                                                                    <Binding Path="Name" RelativeSource="{RelativeSource Self}"/>
                                                                    <Binding Path="ProductNumber"/>
                                                                </MultiBinding>
                                                            </Button.CommandParameter>
                                                        </Button>
                                                        <TextBlock Text="{Binding ProductNumber}"
                                                                   Margin="20,0,5,0"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header=" Description"
                                                            Binding="{Binding ProductDescription}" />
                                        <DataGridTextColumn Header=" Assembly ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="AssemblyQuantity" />
                                                    <Binding Path="ProductUom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>

                                </DataGrid>

                                <!--#endregion-->

                                <!--#region Bill of Material Lot Datagrid -->

                                <Grid Grid.Column="1" Grid.RowSpan="2"
                                      Margin="10,0,0,0">

                                    <Control Background="Transparent"
                                             VerticalAlignment="Center"
                                             HorizontalAlignment="Center"
                                             Margin="0,50,0,0"
                                             Visibility="{Binding LotListLoading, Converter={StaticResource BoolToVisConverter}}"
                                             Style="{DynamicResource BusyAnimationStyle}" />

                                    <TextBlock Text="{Binding LotListText}"
                                               FontSize="24"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding LotList.Count, Converter={StaticResource CountToVisConverter}}"/>

                                    <DataGrid x:Name="dgBomLotResult"
                                              ItemsSource="{Binding ILotResultsList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Top"
                                              MaxWidth="500"
                                              FontSize="18"
                                              FontWeight="Bold"
                                              RowHeaderWidth="0"
                                              AlternatingRowBackground="LightGray"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">
                                        <DataGrid.Visibility>
                                            <MultiBinding Converter="{StaticResource BoolToVisConverter}" ConverterParameter="0*0">
                                                <Binding Path="NoLotResults"/>
                                                <Binding Path="LotListLoading"/>
                                            </MultiBinding>
                                        </DataGrid.Visibility>
                                        <DataGrid.Columns>
                                            <StaticResource ResourceKey="dgBomLotColumn"/>
                                            <DataGridTextColumn Header=" On Hand "
                                                                Binding="{Binding Onhand}">
                                                <DataGridTextColumn.CellStyle>
                                                    <Style TargetType="{x:Type DataGridCell}">
                                                        <Setter Property="Foreground"
                                                                Value="Black" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Onhand, Converter={StaticResource NegIntToBoolConverter}}"
                                                                         Value="true">
                                                                <Setter Property="Foreground"
                                                                        Value="Crimson" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </DataGridTextColumn.CellStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header=" Location "
                                                                Binding="{Binding Location}" />
                                            <StaticResource ResourceKey="dgBomDefectColumn"/>

                                        </DataGrid.Columns>

                                    </DataGrid>

                                </Grid>

                                <!--#endregion-->

                                <!--#region Bill of Material Dedicate Datagrid-->

                                <DataGrid Grid.Column="0" Grid.Row="1"
                                          ItemsSource="{Binding IDedicateLotResultsList}"
                                          Visibility="{Binding NoDedicateResults, ConverterParameter=i, Converter={StaticResource BoolToVisConverter}}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Top"
                                          FontSize="18"
                                          RowHeaderWidth="0"
                                          Margin="0,10,0,0"
                                          FontWeight="Bold"
                                          AlternatingRowBackground="LightGray"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header=" Lot Number "
                                                            Binding="{Binding LotNumber}" />
                                        <DataGridTextColumn Header=" Dedicated ">
                                            <DataGridTextColumn.Binding>
                                                <MultiBinding StringFormat="{}{0}  {1}">
                                                    <Binding Path="Onhand" />
                                                    <Binding Path="Uom" />
                                                </MultiBinding>
                                            </DataGridTextColumn.Binding>
                                            <DataGridTextColumn.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Setter Property="Foreground"
                                                            Value="Black" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Onhand, Converter={StaticResource NegIntToBoolConverter}}"
                                                                     Value="true">
                                                            <Setter Property="Foreground"
                                                                    Value="Crimson" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGridTextColumn.CellStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header=" Location "
                                                            Binding="{Binding Location}" />
                                    </DataGrid.Columns>
                                </DataGrid>

                                <!--#endregion-->

                            </Grid>

                        </TabItem>

                        <!--#endregion-->

                        <!--#region Activity Tab-->

                        <TabItem Header="Activity"
                                 FontSize="18"
                                 Visibility="{Binding Path=(main:CurrentUser.CanSchedule), Converter={StaticResource BoolToVisConverter}}"
                                 IsSelected="{Binding ActSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{DynamicResource ModernTabItem}">

                            <Grid>

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <ComboBox x:Name="PartFilter"
                                          Grid.Row="0"
                                          ItemsSource="{Binding CompCollection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          SelectedItem="{Binding SelectedComp}"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          Background="LightGray"
                                          Width="200"
                                          HorizontalAlignment="Left"
                                          Margin="5"
                                          Style="{DynamicResource RequiredComboBox}"/>
                                
                                <DataGrid x:Name="dgAct"
                                          Grid.Row="1"
                                          ItemsSource="{Binding ActivityTable}"
                                          IsReadOnly="True"
                                          Focusable="False"
                                          FontSize="18"
                                          FontWeight="Bold"
                                          MaxHeight="355"
                                          AlternatingRowBackground="LightGray"
                                          SelectionMode="Single"
                                          RowDetailsVisibilityMode="Visible"
                                          AutoGenerateColumns="False"
                                          RowHeaderWidth="0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Hidden"
                                          ColumnHeaderStyle="{DynamicResource ModernColumnHeader}">

                                    <DataGrid.Resources>
                                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                                         Color="CadetBlue"/>
                                    </DataGrid.Resources>

                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Date"
                                                            Width="120"
                                                            Binding="{Binding DueDate, StringFormat=d}"/>

                                        <DataGridTextColumn Header="Type"
                                                            Width="75"
                                                            Binding="{Binding Type}"/>

                                        <DataGridTextColumn Header="Reference"
                                                            Width="500"
                                                            Binding="{Binding Reference}">
                                            <DataGridTextColumn.CellStyle>
                                                <Style TargetType="DataGridCell">
                                                    <Setter Property="TextBlock.TextAlignment" Value="Left"/>
                                                </Style>
                                            </DataGridTextColumn.CellStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="Quantity"
                                                            Width="100"
                                                            Binding="{Binding Quantity}">
                                            <DataGridTextColumn.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Setter Property="Foreground" Value="Black" />
                                                    <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Quantity, Converter={StaticResource NegIntToBoolConverter}}"
                                                                     Value="true">
                                                            <Setter Property="Foreground"
                                                                    Value="Crimson" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGridTextColumn.CellStyle>
                                        </DataGridTextColumn>

                                        <DataGridTextColumn Header="Balance"
                                                            Width="100"
                                                            Binding="{Binding Balance}">
                                            <DataGridTextColumn.CellStyle>
                                                <Style TargetType="{x:Type DataGridCell}">
                                                    <Setter Property="Foreground" Value="Black" />
                                                    <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Balance, Converter={StaticResource NegIntToBoolConverter}}"
                                                                     Value="true">
                                                            <Setter Property="Foreground"
                                                                    Value="Crimson" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </DataGridTextColumn.CellStyle>
                                        </DataGridTextColumn>

                                    </DataGrid.Columns>

                                    <DataGrid.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                                        </Style>
                                    </DataGrid.CellStyle>

                                </DataGrid>


                            </Grid>

                        </TabItem>

                        <!--#endregion-->

                    </TabControl>

                </Grid>

                <!--#endregion-->

            </Grid>

        </Grid>
    </ScrollViewer>

</UserControl>
