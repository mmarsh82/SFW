<Window x:Class="SFW.UserConfig.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SFW.UserConfig"
        Title="User Config. Changes" Height="450" Width="800">
    <Grid>
        <ComboBox ItemsSource="{Binding MachineList}"
                  DisplayMemberPath="MachineName"
                  SelectedValue="{Binding SelectedMachine, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                  SelectedValuePath="MachineName"
                  Width="200"
                  Height="50"/>
    </Grid>
</Window>
