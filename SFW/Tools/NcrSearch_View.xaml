<Window x:Class="SFW.Tools.NcrSearch_View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SFW.Tools"
        x:Name="NcrSearch_Window"
        Title="NCR Search" Height="150" Width="200" WindowStyle="ToolWindow" Topmost="True">
    <Grid>

        <StackPanel Orientation="Vertical"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <TextBlock Text="NCR Number"
                       FontSize="18"
                       FontWeight="Bold"
                       TextDecorations="Underline"/>

            <TextBox x:Name="UserEntryTextBox"
                     FontSize="18"
                     Margin="5"
                     MaxLength="12"
                     Background="LightGray"
                     Text="{Binding UserEntry, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Search"
                    Margin="5"
                    Width="100"
                    FontSize="18"
                    Command="{Binding SubmitICommand}"
                    Style="{DynamicResource DefaultButton}"/>

        </StackPanel>

    </Grid>
</Window>
